spring:
  application:
    name: currency-exchange-service
  profiles:
    active: dev
  jpa:
    show-sql: true

server:
  port: 8200

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka